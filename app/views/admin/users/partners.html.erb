<div class="main-container">
  <div class="header-container">
    <div class="row">
      <div class="add-new">
      	<%= link_to t('.Back', :default => t("helpers.links.back")),
                    admin_root_path,
                    :class => 'btn btn-primary', style: "color:#fff" %>
        </div>
      <div class="top-title">
        <h1>Partners</h1>
        
      </div>
      
  </div>  

  <table class="table table-striped">
    <thead>
      <tr>
        <th>Name</th>
        <th>City</th>
        <th>Created at</th>
        <th>OverAll Rating</th>
        <th>Actions</th>
      </tr>
    </thead>
<<<<<<< HEAD
    <tbody id="partner_list">
      <%= render partial: 'partner' %>
=======
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= get_full_name(user) %></td>
          <td><%=users_city(user) %></td>
          <td><%=l user.created_at %></td>
          <td><%=get_rating_data(user)%></td>
          <td>
            <%= link_to t('.show', :default => t("helpers.links.show")),
                        partner_details_path(user), :class => 'btn btn-default btn-xs' %>
            <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                        admin_user_path(user),
                        :method => :delete,
                        :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                        :class => 'btn btn-xs btn-danger', style: "color:#fff" %>
            <%= link_to "Service requests", partner_service_request_path(user),  :class => 'btn btn-default btn-xs'  %>
          </td>
        </tr>
      <% end %>
>>>>>>> 8338dbf3843b98d52414fe67b4dcbffde438ea32
    </tbody>
  </table>
</div>